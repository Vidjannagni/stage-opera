# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Environnement & données locales
.env
instance/
*.sqlite3
*.db

# LaTeX
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# Dossier de remise généré
remise/

# Éditeurs / OS
.vscode/
.idea/
.DS_Store

# Visuels non produits par le projet : jamais versionnés, ils appartiennent à
# leurs auteurs. L'application retombe sur son illustration SVG s'ils sont
# absents, si bien que le dépôt et les déploiements restent utilisables.
app/static/img/accueil.*
original-*.webp
Image collée*.png

# Pièces jointes reçues de l'école : ce sont des documents d'entrée, pas des
# productions du projet.
Rapport_stage_acad/Dates soutenances *.zip

# Copies de la base produites par « flask sauvegarder » : ce sont des données
# de clients, elles n'ont rien à faire dans un dépôt.
sauvegardes/
